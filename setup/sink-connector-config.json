{
  "name": "jdbc-sink-connector",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": "1",
    "topics": "dbserver1.dbo.actor,dbserver1.dbo.film,dbserver1.dbo.customer",
    "connection.url": "jdbc:postgresql://postgres:5432/sakila",
    "connection.user": "postgres",
    "connection.password": "Delta@12345678",
    "auto.create": "true",
    "auto.evolve": "true",
    "insert.mode": "insert",
    "pk.mode": "none",
    "batch.size": "3000",
    "max.retries": "10",
    "retry.backoff.ms": "3000"
  }
}
